<UserControl x:Class="Rouyan.Pages.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:s="https://github.com/canton7/Stylet"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="700">
    <Grid Background="#F5F5F5">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="设置" 
                       FontSize="32" 
                       FontWeight="Bold" 
                       Foreground="#2B579A"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>
            
            <StackPanel Width="300" Margin="0,0,0,30">
                <TextBlock Text="主题设置" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                
                <CheckBox Content="深色模式" 
                          Margin="0,0,0,10"/>
                
                <CheckBox Content="自动保存" 
                          Margin="0,0,0,10"/>
                
                <TextBlock Text="语言设置" FontSize="18" FontWeight="Bold" Margin="0,20,0,10"/>
                
                <ComboBox SelectedIndex="0">
                    <ComboBoxItem>中文（简体）</ComboBoxItem>
                    <ComboBoxItem>简体中文</ComboBoxItem>
                </ComboBox>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="🔙 返回首页" 
                        Padding="15,8"
                        Margin="0,0,10,0"
                        Command="{s:Action NavigateToHome}"/>
                        
                <Button Content="ℹ️ 关于我们" 
                        Padding="15,8"
                        Margin="10,0,0,0"
                        Command="{s:Action NavigateToAbout}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>