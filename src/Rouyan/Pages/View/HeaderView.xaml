<UserControl x:Class="Rouyan.Pages.View.HeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Rouyan.Pages.View"
             xmlns:s="https://github.com/canton7/Stylet"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="200">
    
    <Border Background="#2B579A">
        <StackPanel>
            <!-- 应用标题 -->
            <Border Background="#1E3F7A" Height="60">
                <TextBlock Text="Rouyan" 
                           Foreground="White" 
                           FontSize="20" 
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"/>
            </Border>
            
            <!-- 导航菜单 -->
            <StackPanel Margin="0,20,0,0">
                <Button Command="{s:Action NavigateToHome}"
                        Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"
                        Height="45"
                        HorizontalContentAlignment="Left"
                        Margin="0,1" Padding="20,8">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🏠" FontSize="16" Margin="0,0,10,0"/>
                        <TextBlock Text="首页" FontSize="14" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Command="{s:Action NavigateToPromptManagement}"
                        Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"
                        Height="45"
                        HorizontalContentAlignment="Left"
                        Margin="0,1" Padding="20,8">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📝" FontSize="16" Margin="0,0,10,0"/>
                        <TextBlock Text="提示词管理" FontSize="14" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Command="{s:Action NavigateToTerminalAgent}"
                        Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"
                        Height="45"
                        HorizontalContentAlignment="Left"
                        Margin="0,1" Padding="20,8">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🖥️" FontSize="16" Margin="0,0,10,0"/>
                        <TextBlock Text="终端代理" FontSize="14" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Command="{s:Action NavigateToSettings}"
                        Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"
                        Height="45"
                        HorizontalContentAlignment="Left"
                        Margin="0,1" Padding="20,8">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚙️" FontSize="16" Margin="0,0,10,0"/>
                        <TextBlock Text="设置" FontSize="14" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Command="{s:Action NavigateToAbout}"
                        Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"
                        Height="45"
                        HorizontalContentAlignment="Left"
                        Margin="0,1" Padding="20,8">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="ℹ️" FontSize="16" Margin="0,0,10,0"/>
                        <TextBlock Text="关于" FontSize="14" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>