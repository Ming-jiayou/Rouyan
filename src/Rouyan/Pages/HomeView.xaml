<UserControl x:Class="Rouyan.Pages.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:s="https://github.com/canton7/Stylet"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="700">
    <Grid Background="#F5F5F5">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="500">
            <!-- 第一行：获取剪贴板内容按钮 -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                <Button Content="获取剪贴板内容" 
                        Padding="20,10"
                        FontSize="16"
                        Command="{s:Action GetClipboardContent}"
                        HorizontalAlignment="Center"/>
            </StackPanel>
            
            <!-- 第二行：显示剪贴板内容的TextBox -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Width="500">
                <TextBox Text="{Binding ClipboardText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Height="120"
                         Width="500"
                         FontSize="14"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         BorderBrush="#FFABABAB"
                         BorderThickness="1"
                         Padding="10,5"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>